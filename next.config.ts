import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["picsum.photos", "source.unsplash.com", "randomuser.me", "ik.imagekit.io"], // Add Picsum and Unsplash
  },
  /* config options here */
};

export default nextConfig;
